import pygame
from pygame.sprite import Sprite
from pygame.sprite import Group

class Pipe(Sprite):
    def __init__(self):

    # Draw a rectangle around the pipe
    # Rectangle is invisible
    # We can land on the rect
    # When running into pipe, you can't move past it
    # If pipe can be entered
        # If pressed down
            # Enter pipe, change level

    # In the main file, we will create a list of pipes when the
    # Pipes will